create table Book(
                     id int primary key,
                     isbn varchar(13) not null,
                     title varchar(255) not null,
                     author varchar(255) not null
);