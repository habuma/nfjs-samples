server:
  port: 0


management:
  endpoints:
    web:
      exposure:
        include: '*'
        
spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: who-service
    
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/